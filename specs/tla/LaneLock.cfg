\* TLC configuration for LaneLock.tla (spec 014 Phase 1 pilot bounds).
\* Bounds live in the module (2 lanes, 3 agents, 3 paths, 4 lock sets):
\* small by design per the tla-author skill; widen only after this passes.
SPECIFICATION Spec
INVARIANTS
  TypeOK
  Exclusivity
  CoverageForActive
  TerminalClean
  ReviewerSeparation
  ActiveHasLocks
  PrFlowRetention
